## Packages
framer-motion | Page transitions and complex interactive animations
date-fns | Human-readable date formatting for chat messages
zustand | Lightweight global state management for auth and UI state
lucide-react | Beautiful, consistent icons for the UI

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

Colors configured in index.css as HSL variables.
WebSocket connects to /ws path with ?userId query param.
